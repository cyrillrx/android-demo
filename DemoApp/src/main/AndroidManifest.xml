<?xml version="1.0" encoding="utf-8"?>

<manifest
    package="com.cyrilleroux.android.demo"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".DemoActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.cyrilleroux.android.unlock.UnlockActivity"
            android:label="@string/title_unlock"
            android:theme="@style/UnlockTheme"/>

        <activity
            android:name="com.cyrilleroux.android.drawer.SimpleDrawerActivity"
            android:label="@string/title_simple_nav"
            android:theme="@style/SimpleNavTheme" />

        <activity
            android:name="com.cyrilleroux.android.drawer.DoubleDrawerActivity"
            android:label="@string/title_double_nav"
            android:theme="@style/DoubleNavTheme" />

    </application>

</manifest>
